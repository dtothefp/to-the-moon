# One CI gate for the Python workspace (services/api, services/worker, packages/*): lint,
# format check, and tests. Runs on every PR and on merges to main. Kept to a single job so
# "did CI pass" is one green check. Everything runs from the repo root: the uv workspace
# has one lock there, and the root pyproject's testpaths cover every member's tests.
#
# A pgvector Postgres service is stood up so the `integration`-marked tests exercise the
# real app + DB (they auto-skip locally when no DB is reachable). The schema is applied by
# db/upgrade.py, the same entrypoint Railway runs, so the schema drift test in
# packages/core/tests/test_orm_schema.py fails the build whenever the SQLAlchemy models and
# the migrations disagree. No seed is needed; the integration tests don't read seeded rows.

name: ci

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-format-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: lab
          POSTGRES_PASSWORD: lab
          POSTGRES_DB: sysdesign
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U lab -d sysdesign"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DATABASE_URL: postgresql://lab:lab@localhost:5432/sysdesign

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Sync dependencies (installs Python 3.13 + every workspace member + dev group)
        run: uv sync --all-packages

      - name: Lint (ruff check)
        run: uv run ruff check .

      - name: Format check (ruff format)
        run: uv run ruff format --check .

      - name: Install dbmate
        run: |
          sudo curl -fsSL -o /usr/local/bin/dbmate \
            https://github.com/amacneil/dbmate/releases/latest/download/dbmate-linux-amd64
          sudo chmod +x /usr/local/bin/dbmate

      # The same entrypoint Railway's preDeployCommand runs: dbmate history, then alembic.
      # Calling it here means CI exercises the real deploy path, not an approximation of it,
      # and the schema the integration tests run against is the schema prod will have.
      - name: Apply migrations (dbmate history, then alembic)
        run: uv run python packages/core/db/upgrade.py

      - name: Test (pytest, including DB integration tests)
        run: uv run pytest
